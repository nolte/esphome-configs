# ESPHome version used by the config-validation workflow.
#
# Pinned deliberately. An unpinned install would reproduce the very failure
# mode this repository already carries with the untagged
# ghcr.io/esphome/esphome image: an upstream change lands without a single
# line of configuration changing, and an unrelated commit goes red - or worse,
# an API break reaches the devices unnoticed.
#
# Kept in its own file rather than inline in the workflow so Renovate's
# pip_requirements manager picks it up, the same way it already maintains
# requirements-dev.txt.
esphome==2026.8.2
